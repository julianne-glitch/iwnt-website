import { PrismaClient } from '@prisma/client'; import { Pool } from 'pg'; import { PrismaPg } from '@prisma/adapter-pg'; const pool = new Pool({ connectionString: 'postgresql://postgres:postgres@localhost:51214/template1' }); const prisma = new PrismaClient({ adapter: new PrismaPg(pool) }); async function main() { try { const user = await prisma.adminUser.findUnique({ where: { email: 'admin@iwnt.com' } }); console.log(user); } catch (e) { console.error(e); } finally { await prisma.$disconnect(); } } main();
